# Environment Variables for Depth File Organizer
# Copy this file to .env and update with your values

# Source directory (optional, overrides config.yaml)
# SOURCE_DIRECTORY=/path/to/source

# Output directory (optional, overrides config.yaml)
# OUTPUT_DIRECTORY=/path/to/output

# Enable/disable dry-run mode (optional, overrides config.yaml)
# DRY_RUN=true
