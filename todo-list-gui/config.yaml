# To-Do List GUI Configuration

# Database configuration
database:
  file: "data/todo.db"
  create_tables: true

# Logging configuration
logging:
  level: "INFO"
  file: "logs/todo_list.log"
  max_bytes: 10485760
  backup_count: 5
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
