# File Organizer Environment Variables
# Copy this file to .env and update with your values

# Optional: Override source directory from config.yaml
# SOURCE_DIRECTORY=/path/to/downloads

# Optional: Override destination base directory from config.yaml
# DESTINATION_BASE=/path/to/organized

# Optional: Enable dry run mode (preview without moving files)
# DRY_RUN=false
