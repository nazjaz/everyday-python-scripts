# Environment variables for File Activity Organizer
# Copy this file to .env and update with your values

# Optional: Override source directory from environment
# SOURCE_DIRECTORY=/path/to/files

# Optional: Override active directory from environment
# ACTIVE_DIRECTORY=/path/to/active

# Optional: Override archived directory from environment
# ARCHIVED_DIRECTORY=/path/to/archived

# Optional: Override dormant directory from environment
# DORMANT_DIRECTORY=/path/to/dormant
