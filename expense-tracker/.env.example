# Expense Tracker Environment Variables
# Copy this file to .env and update with your values

# Optional: Custom data file path (overrides config.yaml)
# DATA_FILE_PATH=/custom/path/to/expenses.json

# Optional: Custom log level (overrides config.yaml)
# LOG_LEVEL=DEBUG
