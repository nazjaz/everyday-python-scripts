# Regex File Finder Environment Variables
# Copy this file to .env and update with your values

# Search directory (overrides config.yaml)
SEARCH_DIRECTORY=

# Output directory for copy/move operations (overrides config.yaml)
OUTPUT_DIRECTORY=
