# Log Rotator Environment Variables
# Copy this file to .env and update with your values

# Log directory to rotate (overrides config.yaml)
LOG_DIRECTORY=

# Archive directory for rotated logs (overrides config.yaml)
ARCHIVE_DIRECTORY=
