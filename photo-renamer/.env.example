# Photo Renamer Environment Variables
# Copy this file to .env and update with your values

# Optional: Override source directory from config.yaml
# SOURCE_DIRECTORY=/path/to/photos

# Optional: Enable dry run mode (preview without renaming)
# DRY_RUN=false

# Optional: Enable backup creation
# CREATE_BACKUP=false
