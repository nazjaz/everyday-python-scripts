# Image Converter Environment Variables
# Copy this file to .env and update with your values

# Optional: Custom output directory (overrides config.yaml)
# OUTPUT_DIR=/custom/path/to/output

# Optional: Custom log level (overrides config.yaml)
# LOG_LEVEL=DEBUG
