pyyaml==6.0.1  # YAML configuration file parsing
python-dotenv==1.0.0  # Environment variable management
pytest==7.4.3  # Testing framework
python-magic==0.4.27  # MIME type detection library (optional, with fallback)
